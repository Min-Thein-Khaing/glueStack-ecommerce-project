//youtuber using 

npx expo install @expo/ui
npx expo install expo-dev-client
npm run expo:ios


//phone nyo instructor
library react native otp 

npm i react-native-otp-entry




//logic 
when i scroll x 

onScroll = (e)=> {
    const slide = Math.ceil(e.nativeEvent.currentOffset.x / e.nativeEvent.layoutMeasurement.width)
    if(slide !== di kg ka state pay dr nae ){
        state ka setstate ko (slide)
    }
}